import type { Metadata } from "next";
import { TokamaHome } from "@/components/tokama-home/TokamaHome";

export const metadata: Metadata = {
  title: "TOKAMA — domki nad jeziorem | Windyki koło Iławy",
  description: "Trzy całoroczne domki nad jeziorem w Windykach koło Iławy.",
};

export default function HomePage() {
  return <TokamaHome locale="pl" />;
}
